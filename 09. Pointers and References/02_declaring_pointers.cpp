/*

variable_type *pointer_name;

int *int_ptr; // Same as below
int* int_ptr; // Same as above

double *double_ptr;
char *char_ptr;
string *string_ptr;

variable_tupe *pointer_name {nullptr}; // initializing pinter variables to 'point nowhere' // NULL Pointer

- uninitialized pointers contain garbage data and can point anywhere.
- initialiging to zero or nullptr (c++ 11) represents address zero (implies that that pointer is 'pointing nowhere')

*/