#include <iostream>

using namespace std;

int main () {
	ios_base::sync_with_stdio (false);

	int fav_num;
	int a = 10; // never using this var
	cout << fav_num; // never initialised this var

	// return 0; // not returning a value when the function is expacting.
}